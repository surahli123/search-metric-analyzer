{
  "_description": "Persistent learnings from KDD pipeline runs. Updated after each batch.",
  "_format": "question_pattern → {correct_sql_pattern, common_errors, tips}",
  "patterns": {
    "percentage_of_X_with_condition": {
      "correct": "CAST(COUNT(CASE WHEN condition THEN 1 END) AS REAL) * 100 / COUNT(*)",
      "wrong": ["COUNT(*) without CAST (integer division = 0)", "Wrong denominator table"],
      "tip": "Both numerator and denominator COUNT must use the same base table or JOIN"
    },
    "average_monthly_X": {
      "correct": "AVG(column) / 12",
      "wrong": ["SUM(column) / 12 (gives total, not average)", "AVG without /12"],
      "tip": "AVG() computes per-row mean. SUM() computes total across all rows."
    },
    "count_with_group_filter": {
      "correct": "SELECT COUNT(DISTINCT id) FROM ... GROUP BY ... HAVING COUNT(*) > N",
      "wrong": ["WHERE COUNT(*) > N (can't use aggregate in WHERE)"],
      "tip": "Use HAVING for aggregate conditions, WHERE for row conditions"
    },
    "ratio_how_many_times": {
      "correct": "SUM(CASE WHEN A THEN amount END) / SUM(CASE WHEN B THEN amount END)",
      "wrong": ["COUNT(A) - COUNT(B) (difference, not ratio)"],
      "tip": "Ratio = A/B. Difference = A-B. Read the question carefully."
    },
    "lowest_highest_with_name": {
      "correct": "SELECT name FROM table ORDER BY value ASC|DESC LIMIT 1",
      "wrong": ["SELECT MIN(value) (returns value, not the associated name)"],
      "tip": "MIN/MAX returns the value. To get the associated row, use ORDER BY + LIMIT 1."
    },
    "join_for_human_readable_names": {
      "correct": "JOIN the reference table to get names instead of IDs",
      "wrong": ["Returning raw IDs like 'rec4BLdZHS2Blfp4v' instead of names"],
      "tip": "If the question asks for names/descriptions, JOIN to get them."
    },
    "filter_with_domain_knowledge": {
      "correct": "Read knowledge.md to map question terms to exact column values. E.g., 'severe thrombosis' = Thrombosis=2, 'abnormal creatinine' needs the specific threshold from knowledge.md",
      "wrong": ["Guessing filter values without reading knowledge.md", "Using wrong column for a condition (e.g., filtering on Patient table when the value is in Examination table)"],
      "tip": "When the question uses domain terms (severe, abnormal, normal), ALWAYS check knowledge.md for the exact mapping to column values and which table contains the column."
    },
    "multi_table_filter_chain": {
      "correct": "Join ALL tables needed to satisfy ALL conditions. E.g., 'members from Illinois' needs member → zip_code JOIN to access state column",
      "wrong": ["Assuming a filter column exists in the main table when it's in a reference table", "Missing a JOIN and getting 0 results or wrong results"],
      "tip": "Before writing SQL, list every condition in the question and verify which table has each column. If a condition's column is in a different table, you MUST JOIN."
    },
    "airtable_style_foreign_keys": {
      "correct": "Columns named 'link_to_X' are foreign keys to table X's primary key (usually X_id or member_id). JOIN on link_to_X = X.primary_key",
      "wrong": ["Ignoring link_to_* columns and trying to filter without JOINing", "Treating opaque string IDs like 'rec4BLdZHS2Blfp4v' as displayable values instead of FK references"],
      "tip": "When you see a column named 'link_to_budget', 'link_to_member', 'link_to_event' etc., it is a foreign key. JOIN to that table to access its columns."
    },
    "date_time_filtering": {
      "correct": "For 'June 2013': Date LIKE '2013-06%' or strftime('%Y-%m', Date) = '2013-06'. For year+name: JOIN to reference table with year and name columns",
      "wrong": ["Using wrong date format (MM/YYYY vs YYYY-MM-DD)", "Comparing time strings lexicographically ('1:26' < '9:05' is wrong numerically)"],
      "tip": "Always check the actual date format in sample data before writing WHERE clauses. For time comparisons, CAST to numeric or use MIN/MAX with ORDER BY."
    },
    "multi_row_matching": {
      "correct": "When the question mentions multiple items (water + veggie tray + supplies), match across MULTIPLE rows per group using separate LIKE conditions ORed together, then GROUP BY + HAVING COUNT(DISTINCT matched_item) >= N",
      "wrong": ["Trying to match all items in a single row with AND (fails when items are in separate rows)"],
      "tip": "If items can be in separate rows, use OR to match any, then GROUP BY the entity and HAVING to ensure all items were found."
    }
  }
}
