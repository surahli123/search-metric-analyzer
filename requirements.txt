# Search Metric Analyzer — dependencies
# Core (required for all usage)
pyyaml>=6.0       # Parse metric_definitions.yaml and historical_patterns.yaml

# Testing
pytest>=7.0        # Test runner
pytest-cov>=4.0    # Coverage reporting

# Web backend (required for web app)
fastapi>=0.100.0   # Web API framework
uvicorn>=0.23.0    # ASGI server for FastAPI

# LLM integration (optional — only needed for real pipeline runs)
# anthropic>=0.25.0  # Uncomment when running real investigations via Anthropic API
# openai>=1.0.0      # Uncomment when running real investigations via OpenAI-compatible API (Novita, Together, Groq, etc.)
